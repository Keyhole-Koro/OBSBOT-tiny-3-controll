# OBSBOT Tiny 3 Controller

Python scripts to control the OBSBOT Tiny 3 over OSC (TCP).

## Requirements
```bash
pip install -r requirements.txt
```

## Usage
Run the keyboard controller for interactive W/A/S/D control and P for snapshot (saves to Downloads folder).
```bash
python obsbot_keyboard_controller.py
```
